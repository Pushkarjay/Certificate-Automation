{
  "name": "certificate-automation",
  "version": "1.0.0",
  "description": "Certificate Automation System - Monorepo",
  "private": true,
  "scripts": {
    "build": "cd Frontend/React && npm install && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd .. && cd Frontend/React && npm install && cd ../..",
    "install:backend": "cd backend && npm install && cd ..",
    "install:frontend": "cd Frontend/React && npm install && cd ../..",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd Frontend/React && npm start",
    "clean": "rm -rf backend/node_modules Frontend/React/node_modules",
    "clean:win": "rmdir /s /q backend\\node_modules Frontend\\React\\node_modules"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "certificate",
    "automation",
    "nodejs",
    "react",
    "fullstack",
    "monorepo"
  ],
  "author": "SURE Trust",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
